{
  "name": "wedding-countdown-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.2",
    "resend": "^4.6.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
